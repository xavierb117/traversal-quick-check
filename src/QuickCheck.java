public class QuickCheck {

  // Print all strings longer than 7 characters in a tree
  // Each string should be printed on a separate line, in pre-order
  // If the node is null, print nothing
  // You can assume there are no null strings in the values of the nodes.
  public void printLongWords(TreeNode<String> node) {

  }

  // Return the sum of all odd-valued nodes in the tree
  // If the node is null, return 0
  public int oddSum(TreeNode<String> node) {
    return -1;
  }
}